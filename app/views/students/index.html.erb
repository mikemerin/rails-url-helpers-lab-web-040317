<h1>All students</h1>

<div>
  <% @students.each do |student| %>
    <div><%= link_to student.to_s, student_path(student) %></div>
  <% end %>
</div>
<br>

<!-- <%= link_to "Create a student", student_path('new') %> -->
